{
  "name": "url-spy",
  "version": "0.1.0",
  "description": "url spy, listen the url when it change",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"configless -e ./index -o ./build --library urlSpy --libraryTarget umd --watch",
    "build": "configless -e ./index -o ./build --library urlSpy --libraryTarget umd",
    "preinstall": "webpack-configless --version"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/axetroy/webpack-configless-boilerplate.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/axetroy/webpack-configless-boilerplate/issues"
  },
  "homepage": "https://github.com/axetroy/webpack-configless-boilerplate#readme",
  "dependencies": {
    "@axetroy/event-emitter.js": "^1.2.4"
  }
}
